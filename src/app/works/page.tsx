import Image from 'next/image'; import Link from 'next/link'; import {caseStudies} from '@/data/cases';
export default function WorksPage(){return <main className="page-shell"><header className="page-intro"><h1><small>02 / CASE STUDIES</small>Work</h1><div><p>不動産、組織、DX、地域協業。領域は異なっても、担っているのは「複雑な条件を読み、関係者とともに実行可能な形へ移す」仕事です。</p><p className="eyebrow">PUBLIC INFORMATION ONLY — DETAILS ARE ABSTRACTED</p></div></header><section className="case-grid" aria-label="ケーススタディ一覧">{caseStudies.map(c=><Link href={`/works/${c.id}`} className="case-card" key={c.id}><span className="work-num">{c.number}</span><figure><Image src={c.image} alt="" fill sizes="(max-width: 760px) 90vw, 35vw"/></figure><div><p className="eyebrow">{c.category}</p><h2>{c.title}</h2><p>{c.lead}</p><div className="tags">{c.skills.map(s=><span key={s}>{s}</span>)}</div></div></Link>)}</section></main>}
