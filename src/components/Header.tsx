'use client';
import Link from 'next/link'; import {useState} from 'react';
export default function Header({textColor: _textColor}:{textColor?:string}){const [open,setOpen]=useState(false);return <header className="site-header"><Link className="brand" href="/">馬場 雄大<small>YUDAI BABA</small></Link><button className="menu-button" aria-expanded={open} aria-controls="global-nav" onClick={()=>setOpen(!open)}>{open?'CLOSE':'MENU'}</button><nav id="global-nav" className={`site-nav ${open?'open':''}`} aria-label="メインナビゲーション">{[["/","INDEX"],["/works","WORK"],["/photos","FIELD NOTES"],["/about","ABOUT"],["/contact","CONTACT"]].map(([href,label])=><Link key={href} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</nav></header>}
